numpy>=2.0
scipy>=1.13
matplotlib>=3.8
PyYAML>=6.0
imageio>=2.34
pillow>=10.0
pytest>=8.0
pybind11>=3.0
scikit-build-core>=0.10
# Optional GPU backend for the reduced MHD kernels. Install the wheel matching
# your CUDA/Python stack, for example cupy-cuda12x or cupy-cuda13x when available.
# cupy-cuda12x>=13.0
